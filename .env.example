# GitHub Authentication
# Create a personal access token at: https://github.com/settings/tokens
# Required scopes: repo, read:org (for private repos), public_repo (for public repos)
GITHUB_TOKEN=ghp_your_token_here

# LexRunner Configuration
# LEX_PR_PROFILE_DIR=/path/to/custom/profile
# ALLOW_MUTATIONS=true
