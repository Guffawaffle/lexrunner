# Governed review synthetic corpus

This corpus is deliberately inert. It contains a base and candidate version of one small function so
the disposable provider can prove read-only comparison, structured output, and refusal without
mounting a real repository.
