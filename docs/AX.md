# AX (Agent Experience) contract

AX makes LexRunner operations predictable for both humans and agents. A machine-facing operation
needs bounded structured output, recoverable stable errors, and one semantic owner even when CLI
and MCP expose different transport shapes.

Full command-for-tool symmetry is not the goal. Interactive review, ideation, terminal navigation,
and local administration may remain human-only. `workflow.guide` is intentionally MCP-only. Where
parity is promised, both adapters call the same application service.

## Guarantees

### Bounded structured output

Canonical machine operations use one of the contract profiles recorded in
[`architecture/cli-mcp-surface.json`](architecture/cli-mcp-surface.json):

- `bounded-ax-v1` — bounded success data, stable operation/schema identity, artifact references
  for large output, and AXError failures with recovery actions;
- `agent-work-handler-v1` — bounded JSON-safe projections of durable Attempt lifecycle bodies; or
- `human-local-v1` — explicitly interactive/local output with no MCP parity promise.

Human formatting is a projection. It must not become a second implementation of plan, gate,
workspace, merge, or Attempt semantics.

### Recoverable errors

Machine failures carry a stable code, bounded message/context, and at least one next action. An
agent should be able to distinguish invalid input, authorization refusal, test failure, timeout,
external infrastructure failure, and inconclusive verification without parsing prose or logs.

### Semantic parity

Each approved CLI/MCP pair has one owning application service. Adapters validate and bound input,
call the owner, project the result, and map failures. They do not implement lifecycle transitions.

`WorkItem`, `Run`, and `Attempt` are ADR-010 coordination nouns. `IntegrationRun` is a deprecated
frozen integration-record noun and never orchestration authority. Plan/Gate/Weave remain owned by
the stateless integration core.

## Registration-verified surface

The following block is generated by `npm run docs:surface` from the checked-in matrix. The matrix
itself is tested against `inspectRegisteredCliSurface()` and the published MCP `tools/list` response.

<!-- BEGIN GENERATED AX SURFACE -->

Matrix schema: **1.0.0**. Live inventory: **139 CLI registrations** and **33 MCP tools**.

| Surface | Canonical | Compatibility | Deprecated | Internal only | Remove |
| ------- | --------: | ------------: | ---------: | ------------: | -----: |
| CLI     |       100 |             9 |         15 |            15 |      0 |
| MCP     |        29 |             0 |          4 |             0 |      0 |

### Canonical CLI/MCP semantic pairs

| MCP tool                                   | CLI operation                             | Owning application service              |
| ------------------------------------------ | ----------------------------------------- | --------------------------------------- |
| `accept_attempt`                           | `lexrunner attempt acceptance apply`      | `AgentWorkAttemptAcceptanceService`     |
| `attach_attempt_worker`                    | `lexrunner attempt worker attach`         | `AgentWorkWorkerSessionService`         |
| `cleanup_native_wsl_projection`            | `lexrunner attempt projection cleanup`    | `NativeWslProjectionLifecycleHandlers`  |
| `config.show`                              | `lexrunner config show`                   | `ConfigurationQueryService`             |
| `discover`                                 | `lexrunner weave discover`                | `DiscoveryQueryService`                 |
| `doctor`                                   | `lexrunner workspace doctor`              | `WorkspaceDiagnosticsService`           |
| `end_attempt_worker`                       | `lexrunner attempt worker end`            | `AgentWorkWorkerSessionService`         |
| `gates.run`                                | `lexrunner gate run`                      | `GateExecutionService`                  |
| `get_attempt_acceptance`                   | `lexrunner attempt acceptance status`     | `AgentWorkAttemptAcceptanceService`     |
| `get_attempt_receipt`                      | `lexrunner attempt receipt status`        | `AgentWorkAttemptReceiptService`        |
| `get_attempt_status`                       | `lexrunner attempt status`                | `AgentWorkLifecycleService`             |
| `get_attempt_verification`                 | `lexrunner attempt verification status`   | `AgentWorkAttemptVerificationService`   |
| `get_attempt_worker`                       | `lexrunner attempt worker status`         | `AgentWorkWorkerSessionService`         |
| `get_native_wsl_projection_status`         | `lexrunner attempt projection status`     | `NativeWslProjectionLifecycleHandlers`  |
| `heartbeat_attempt_worker`                 | `lexrunner attempt worker heartbeat`      | `AgentWorkWorkerSessionService`         |
| `local.init`                               | `lexrunner workspace init`                | `WorkspaceInitializationService`        |
| `merge-order`                              | `lexrunner weave merge-order`             | `MergeOrderQueryService`                |
| `merge.apply`                              | `lexrunner weave apply`                   | `MergeApplicationService`               |
| `plan.create`                              | `lexrunner weave plan`                    | `PlanCreationService`                   |
| `preflight_attempt_containment`            | `lexrunner attempt preflight`             | `AgentWorkContainmentCapabilityService` |
| `prepare_native_wsl_projection`            | `lexrunner attempt projection prepare`    | `NativeWslProjectionLifecycleHandlers`  |
| `prepare_attempt`                          | `lexrunner attempt prepare`               | `AgentWorkLifecycleService`             |
| `profile.resolve`                          | `lexrunner config show`                   | `ConfigurationQueryService`             |
| `start_attempt`                            | `lexrunner attempt start`                 | `AgentWorkLifecycleService`             |
| `status`                                   | `lexrunner weave status`                  | `IntegrationStatusQueryService`         |
| `submit_attempt_receipt`                   | `lexrunner attempt receipt submit`        | `AgentWorkAttemptReceiptService`        |
| `inspect_native_wsl_projection_quarantine` | `lexrunner attempt projection quarantine` | `NativeWslProjectionLifecycleHandlers`  |
| `verify_attempt`                           | `lexrunner attempt verification run`      | `AgentWorkAttemptVerificationService`   |

### Intentional parity exceptions

- MCP `workflow.guide`: agent-only workflow guidance; humans use CLI help and checked-in documentation
- CLI `lexrunner plan-review`: interactive human approval is intentionally not exposed as an MCP mutation
- CLI `lexrunner idea`: human ideation and GitHub issue creation remain an explicit local workflow
- CLI `lexrunner view`: terminal-interactive plan navigation has no machine-facing semantics

### Published deprecated MCP tools

- `lexrunner.getStatus` → status for integration; get_attempt_status for ADR-010 orchestration; remove in 2.0.0.
- `lexrunner.listArtifacts` → bounded artifact references from the owning plan.create, gates.run, or merge.apply operation; remove in 2.0.0.
- `lexrunner.startRun` → plan.create, gates.run, or merge.apply for integration; start_attempt for ADR-010 orchestration; remove in 2.0.0.
- `health` → doctor; remove in 2.0.0.

<!-- END GENERATED AX SURFACE -->

## Disposition meanings

- **Canonical:** supported vocabulary with a declared output contract.
- **Compatibility:** retained for a distinct existing caller with an explicit replacement.
- **Deprecated:** warns on use and carries a removal release.
- **Internal only:** developer/governance machinery, not a public parity promise.
- **Remove:** a live registration approved for deletion without replacement.

The complete per-registration classification—including CLI-only operations—is kept in the JSON
matrix instead of duplicated here.

## Verification

```bash
npm run docs:surface:check
npx vitest run tests/architecture/cli-mcp-surface.spec.ts tests/cli/alias-policy.spec.ts
```

The first command prevents this page and README’s displayed package version from drifting. The
tests prove every live CLI registration and every published MCP tool has exactly one disposition,
that canonical MCP tools have either a semantic CLI owner or an explicit exception, and that
compatibility warnings agree with the matrix.

## Related documentation

- [Canonical CLI/MCP surface](architecture/canonical-cli-mcp-surface.md)
- [CLI alias migration policy](architecture/cli-alias-policy.md)
- [IntegrationRun compatibility](architecture/integration-run-compatibility.md)
- [Implementation gate contract](architecture/implementation-gates.md)
- [ADR-010 orchestration protocol](adr/ADR-010-agent-work-orchestration-protocol.md)
- [MCP server](../README.mcp.md)
